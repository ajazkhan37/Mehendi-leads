# TODO

## Mehendi-leads
- [x] Fix Python 3.14 crash (`ast.Str` removal) for Render via compatibility shim.
- [x] Pin/ensure dependency versions for stable Flask/Werkzeug.
- [x] Ensure Procfile uses gunicorn.
- [ ] Confirm Render deployment succeeds and app reaches Live.
- [ ] Run `python db_init.py` on deploy or as part of build if Render requires it.

